package:
  format-version: "1.0.0"
  name: "bootloader_apps_uart"
  version: "v3.4.0"
  type: "application"
  status: "production"
  required: false

  dependencies:
    - name: "bootloader"
      type: "package"
      version: "v3.8.0"
